# Security
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database: sqlite (default) or mysql
DB_ENGINE=sqlite

# MySQL settings (used only when DB_ENGINE=mysql)
DB_NAME=orainauth
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
